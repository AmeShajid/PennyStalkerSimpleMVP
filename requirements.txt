# Core dependencies for web scraping and data extraction
requests==2.31.0
beautifulsoup4==4.12.2
lxml==5.1.0

# Environment variable management
python-dotenv==1.0.0

# Optional: Better date/time handling (if needed later)
python-dateutil==2.8.2